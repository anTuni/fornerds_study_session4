# 보안감사 실습 결과 정리 양식

## 1. 기본 정보

| 항목 | 내용 |
| --- | --- |
| 팀 이름 |  |
| 작성일 |  |
| 수비자 A |  |
| 수비자 B |  |
| 실습 저장소 |  |
| 최종 공유 PR 또는 브랜치 |  |

## 2. 실습 목표

이번 실습에서 검증하려는 목표를 적습니다.

- 자동화된 보안감사 job이 PR에서 실행되는지 확인한다.
- OWASP Top 10 유형의 취약점 PR을 탐지한다.
- 탐지 결과를 보고서 또는 PR 댓글로 남긴다.
- 취약점 발견 시 PR 머지를 방지하거나 경고한다.
- 반복 실습 결과를 바탕으로 표준 보안감사 workflow 후보를 만든다.

## 3. 역할 및 브랜치

| 사람 | 수비 브랜치 | 공격 브랜치 | 공격 PR 대상 | PR 링크 |
| --- | --- | --- | --- | --- |
|  | `defend/` | `attack/` | `defend/` |  |
|  | `defend/` | `attack/` | `defend/` |  |

## 4. 수비자가 만든 보안감사 Workflow 요약

| 항목 | 내용 |
| --- | --- |
| workflow 파일 경로 | `.github/workflows/` |
| 실행 트리거 | `pull_request` to `defend/...` |
| 사용한 도구 | 예: Maven test, npm audit, Semgrep, Trivy, Snyk, CodeQL |
| 보고서 생성 방식 | 예: artifact upload, PR comment, job summary |
| 실패 기준 | 예: critical/high 발견 시 실패 |
| PR 차단 방식 | 예: required status check, branch protection |
| 평균 실행 시간 |  |
| 가장 느린 단계 |  |

### Workflow 핵심 설정

```yaml
on:
  pull_request:
    branches:
      - defend/{name}
```

중요한 설정이나 판단 근거를 적습니다.

- 
- 
- 

## 5. 공격/수비 사이클 기록

### Cycle 1

| 항목 | 내용 |
| --- | --- |
| 공격자 |  |
| 수비자 |  |
| 공격 브랜치 | `attack/` |
| 방어 브랜치 | `defend/` |
| PR 링크 |  |
| 취약점 유형 | 예: A01 Broken Access Control, A03 Injection, A06 Vulnerable Components |
| 수정 파일 |  |
| 취약점 설명 |  |
| 기대 탐지 결과 |  |

#### 실행 결과

| 점검 항목 | 결과 |
| --- | --- |
| workflow 실행 여부 | 성공 / 실패 |
| 취약점 탐지 여부 | 탐지 / 미탐지 / 오탐 |
| 보고서 생성 여부 | 생성 / 미생성 |
| PR 댓글 여부 | 생성 / 미생성 |
| job 실패 여부 | 실패 / 성공 |
| PR 머지 차단 여부 | 차단 / 차단 안 됨 |
| 실행 시간 |  |

#### 탐지 결과 요약

```text
탐지된 파일/라인:

탐지 메시지:

```

#### Cycle 1에서 개선한 점

- 
- 

### Cycle 2

| 항목 | 내용 |
| --- | --- |
| 공격자 |  |
| 수비자 |  |
| 공격 브랜치 | `attack/` |
| 방어 브랜치 | `defend/` |
| PR 링크 |  |
| 취약점 유형 |  |
| 수정 파일 |  |
| 취약점 설명 |  |
| 기대 탐지 결과 |  |

#### 실행 결과

| 점검 항목 | 결과 |
| --- | --- |
| workflow 실행 여부 | 성공 / 실패 |
| 취약점 탐지 여부 | 탐지 / 미탐지 / 오탐 |
| 보고서 생성 여부 | 생성 / 미생성 |
| PR 댓글 여부 | 생성 / 미생성 |
| job 실패 여부 | 실패 / 성공 |
| PR 머지 차단 여부 | 차단 / 차단 안 됨 |
| 실행 시간 |  |

#### 탐지 결과 요약

```text
탐지된 파일/라인:

탐지 메시지:

```

#### Cycle 2에서 개선한 점

- 
- 

### Cycle 3

| 항목 | 내용 |
| --- | --- |
| 공격자 |  |
| 수비자 |  |
| 공격 브랜치 | `attack/` |
| 방어 브랜치 | `defend/` |
| PR 링크 |  |
| 취약점 유형 |  |
| 수정 파일 |  |
| 취약점 설명 |  |
| 기대 탐지 결과 |  |

#### 실행 결과

| 점검 항목 | 결과 |
| --- | --- |
| workflow 실행 여부 | 성공 / 실패 |
| 취약점 탐지 여부 | 탐지 / 미탐지 / 오탐 |
| 보고서 생성 여부 | 생성 / 미생성 |
| PR 댓글 여부 | 생성 / 미생성 |
| job 실패 여부 | 실패 / 성공 |
| PR 머지 차단 여부 | 차단 / 차단 안 됨 |
| 실행 시간 |  |

#### 탐지 결과 요약

```text
탐지된 파일/라인:

탐지 메시지:

```

#### Cycle 3에서 개선한 점

- 
- 

## 6. 탐지 성공 사례

| 취약점 유형 | 탐지 도구 또는 규칙 | 탐지 파일 | 왜 잘 탐지됐는가 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

## 7. 탐지 실패 또는 오탐 사례

| 유형 | 파일 또는 상황 | 원인 분석 | 다음 개선안 |
| --- | --- | --- | --- |
| 미탐 |  |  |  |
| 오탐 |  |  |  |

## 8. PR 차단 검증

| 항목 | 결과 |
| --- | --- |
| 실패한 check 이름 |  |
| branch protection 설정 여부 | 설정 / 미설정 |
| required status check 이름 |  |
| PR 상태 | 예: BLOCKED, UNSTABLE, MERGEABLE |
| 실제 머지 버튼 상태 |  |

정리:

- workflow가 실패해도 branch protection이 없으면 실제 머지는 가능할 수 있다.
- PR을 확실히 막으려면 required status check 설정이 필요하다.
- 

## 9. Lesson Learned

### 기술적으로 배운 점

- 
- 
- 

### workflow 설계에서 배운 점

- 
- 
- 

### 팀 협업에서 배운 점

- 
- 
- 

## 10. 표준 보안감사 Workflow 제안

팀에서 최종 표준 workflow에 포함해야 한다고 생각하는 항목을 정리합니다.

| 우선순위 | 항목 | 포함 여부 | 이유 |
| --- | --- | --- | --- |
| 필수 | Backend test |  |  |
| 필수 | Frontend build |  |  |
| 필수 | 의존성 취약점 스캔 |  |  |
| 필수 | SAST |  |  |
| 권장 | Secret scan |  |  |
| 권장 | Container 또는 filesystem scan |  |  |
| 권장 | PR 댓글 보고서 |  |  |
| 권장 | Artifact 업로드 |  |  |
| 필수 | Required status check |  |  |

### 제안하는 실패 기준

- Critical: 
- High: 
- Medium: 
- Low: 

### 제안하는 보고서 형식

- PR 댓글에 포함할 내용:
- Artifact로 남길 내용:
- 팀 회고에서 공유할 내용:

## 11. 최종 결론

이번 실습에서 만든 workflow를 표준으로 채택할지 판단합니다.

| 항목 | 판단 |
| --- | --- |
| 표준 workflow로 채택 가능 여부 | 가능 / 보완 필요 / 불가 |
| 바로 적용 가능한 부분 |  |
| 추가 실험이 필요한 부분 |  |
| 다음 액션 |  |

최종 의견:

-
